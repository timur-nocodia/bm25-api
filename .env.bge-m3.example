# Docker Configuration
CONTAINER_NAME=bge-m3-api
HOST_PORT=8080
CONTAINER_PORT=8080
CACHE_PATH=/cache

# BGE-M3 Configuration
USE_BGE_M3=true
DENSE_MODEL=BAAI/bge-m3
USE_FP16=false  # Set to true for GPU deployment or memory optimization

# Application Configuration
APP_HOST=0.0.0.0
WORKERS=1
LOG_LEVEL=info
BATCH_SIZE_DEFAULT=32  # Smaller batch size recommended for BGE-M3
THREADS_DEFAULT=1

# Performance Settings (increased for BGE-M3)
OMP_NUM_THREADS=2
OPENBLAS_NUM_THREADS=2
MKL_NUM_THREADS=2

# Resource Limits (significantly increased for BGE-M3)
CPU_LIMIT=4.0
MEMORY_LIMIT=4G
CPU_RESERVATION=2.0
MEMORY_RESERVATION=2G

# Restart Policy
RESTART_POLICY=always
RESTART_CONDITION=any
RESTART_DELAY=10s
RESTART_MAX_ATTEMPTS=0
RESTART_WINDOW=180s

# Health Check Settings (adjusted for BGE-M3)
HEALTH_CHECK_INTERVAL=60s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3
HEALTH_CHECK_START_PERIOD=120s  # Longer startup time for model loading

# Security Settings
API_KEY=your_secret_api_key_here

# BGE-M3 Features Available:
# 1. Dense embeddings (1024 dimensions)
# 2. Sparse embeddings (lexical/keyword matching)  
# 3. ColBERT multi-vector embeddings (optional)
# 4. 100+ languages including Russian, Chinese, English
# 5. Up to 8192 token input length
# 6. Multi-functionality in single model